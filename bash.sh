# Use these commands to see all running processes. 
# This will allow you to uninstall any processes that aren't necessary.
# systemd-analyze blame
# systemd-analyze critical chain
# sudo apt uninstall <name_of_app>

# Installs necessary for python-opencv
sudo apt-get install libhdf5-dev libatlas-base-dev libjasper-dev libgtk2.0-dev pkg-config -y
